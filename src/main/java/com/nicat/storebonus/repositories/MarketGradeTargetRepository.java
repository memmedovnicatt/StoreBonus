package com.nicat.storebonus.repositories;

import com.nicat.storebonus.entities.MarketGradeTarget;
import org.springframework.data.jpa.repository.JpaRepository;

public interface MarketGradeTargetRepository extends JpaRepository<MarketGradeTarget, Long> {
}
